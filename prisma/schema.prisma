generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Authentication models
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  password      String
  isWhitelisted Boolean   @default(false)
  createdAt     DateTime  @default(now())
  lastLogin     DateTime?
  
  sessions      Session[]
  
  @@map("users")
}

model Session {
  id           String   @id @default(cuid())
  userId       String
  createdAt    DateTime @default(now())
  expiresAt    DateTime
  lastActivity DateTime @default(now())
  
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@map("sessions")
}

model Invention {
  // Primary key - passed separately
  asset_id                  String                      @id
  
  // Mandatory fields (3 required)
  inventiontitle            String
  commonname                String
  inventordetails           String
  
  // Optional fields - Invention Details
  rating                    Int?                        @default(0)
  
  // Optional fields - Entity Details
  entity                    String?
  date                      String?
  inventioncountry          String?
  creationcountry           String?
  collaboration             String?
  collaboratorName          String?
  collaboratorCountry       String?
  stakeholders              String?
  entityJournalNumbers      String?
  entityProductIdentity     String?
  
  // Optional fields - Technology Details
  incrementalrenovation     String?
  patentnumbers             String?
  journalnumbers            String?
  productidentity           String?
  problemaddressed          String?
  
  // Optional fields - Train Run / Experimentation
  trainrun                  String?
  experimentresults         String?
  evidence                  String[]                    @default([])
  minuteOfMeeting           String[]                    @default([])
  attachments               String[]                    @default([])
  
  // Optional fields - Effort Sheet Details (matching component field names)
  iprecognizer              String?
  hoursspent                Int?        // Integer for hours
  agencyrecognizer          String?
  agencycost                Int?        // Integer for cost
  revieweffort              Int?        // Integer for effort
  managerempid              String?
  extractionEffort          Int?        // Integer for effort
  
  // Optional fields - Activity Status
  activityStatus            String?
  
  // Optional fields - Inventors (stored as JSON)
  inventors                 Json?
  
  // Timestamps
  created_at                DateTime                    @default(now())
  updated_at                DateTime                    @updatedAt
  
  // Relations
  extractions               Extraction[]                @relation("InventionExtraction")
  patentabilities           Patentability[]             @relation("InventionPatentabilities")
  patentabilityAnalyses     PatentabilityAnalysis[]     @relation("InventionPatentabilityAnalyses")
  PatentSpecificInformation PatentSpecificInformation[] @relation("PatentSpecificInfoToInvention")
  PatentFiling              PatentFiling[]              @relation("PatentFiling")
  PostGrantOpposition       PostGrantOpposition[]       @relation("PostGrantOppositionToInvention")
  PatentProsecution         PatentProsecution[]         @relation("PatentProsecution")
  PatentMaintenance          PatentMaintenance[]          @relation("PatentMaintenanceToInvention")
  patentCommercialisation   PatentCommercialisation[]   @relation("PatentCommercialisation")
}

model Extraction {
  id               Int        @id @default(autoincrement())
  asset_id         String     // Required foreign key
  invention        Invention  @relation("InventionExtraction", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  // Extraction Details (matching V2 component field names exactly)
  extractorOne     String?
  extractortwo     String?
  iEDate           String?
  iawpl            String?
  nfeature         String?
  ifeature         String?
  idattachments    String[] @default([])
  scountry         String?
  oextractor       String?
  
  // NBA Updates
  updatenba        String?
  
  // Effort Sheet Details (matching component field names exactly)
  iprecognizer     String?
  hoursspent       Int?        // Integer for hours
  agencyrecognizer String?
  agencycost       Int?        // Integer for cost
  revieweffort     Int?        // Integer for effort
  managerempid     String?
  extractionEffort Int?        // Integer for effort
  
  // Activity Status
  activityStatus   String?
  
  // Timestamps
  createdAt        DateTime   @default(now())
  updatedAt        DateTime   @updatedAt
}

model Patentability {
  id Int @id @default(autoincrement())

  // Foreign key to Invention
  asset_id  String
  invention Invention @relation("InventionPatentabilities", fields: [asset_id], references: [asset_id], onDelete: Cascade)

  // Extractor Details
  psone       String?
  pstwo       String?
  rating      Int?
  nfeature    String?
  ifeature    String?
  scountry    String?
  oextractor String?

  // Invention Details
  trainRun String?

  // Decision Sheet
  nodc       String?
  dibrief    String?
  attachment String[] @default([])

  // Effort Sheet-6
  esfsearcher      String?
  ipRecognizer     String?
  hoursSpent       String?
  agencyRecognizer String?
  agencyCost       String?
  reviewEffort     String?
  managerEmpId     String?

  // Activity Status
  activityStatus String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model PatentabilityAnalysis {
  id               Int        @id @default(autoincrement())
  asset_id         String     // Required foreign key
  invention        Invention  @relation("InventionPatentabilityAnalyses", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  // Patentability Analysis fields
  analysisType           String?
  analysisDate           String?
  priorArtScope          String?
  patentabilityRating    String?
  analysisSummary        String?
  patentReferences       String?
  nonPatentLiterature    String?
  priorArtSearch         String?
  noveltyAssessment      String?
  inventiveStep          String?
  industrialApplicability String?
  patentabilityConclusion String?
  recommendations        String?
  riskAssessment         String?
  competitorAnalysis     String?
  marketPotential        String?
  technicalFeasibility   String?
  legalCompliance        String?
  costBenefitAnalysis    String?
  timelineEstimate       String?
  nextSteps              String?
  
  // Extractor Details fields
  extractorOne     String?
  extractortwo     String?
  iEDate           String?
  iawpl            String?
  nfeature         String?
  ifeature         String?
  idattachments    String[] @default([])
  scountry         String?
  oextractor       String?
  
  // Innovation Analysis fields
  trainRun         String?
  minuteOfMeeting  String[] @default([])
  attachments      String[] @default([])
  
  // Decision Sheet fields
  nodc             String?
  dibrief          String?
  
  // Patentability Extractor fields
  psone            String?
  pstwo            String?
  rating           Int?
  collaboration    String?
  
  // Effort Sheet fields
  iprecognizer    String?
  hoursspent      Int?
  agencyrecognizer String?
  agencycost      Int?
  revieweffort    Int?
  managerempid    String?
  extractionEffort Int?
  
  // Activity Status
  activityStatus  String?
  
  // Timestamps
  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt
}

model PatentSpecificInformation {
  id             String    @id @default(cuid()) // Primary key
  asset_id       String // Foreign key
  activityStatus String?
  rating         Int?
  draftType      String?
  data           Json?
  
  // National Phase fields
  npPCTDate                    String?
  npApplicationNumber          String?
  npPCTPublication            String?
  npSearchReport              String[] @default([])
  npPCTOrProvisionalDate      String?
  npApplicationCountry        String?
  npDrafterName               String?
  npClaimSheet                String[] @default([])
  npFormsPrepared             String?
  npCountryFiling             String?
  npReviewBy                  String?
  npCitedPatent               String?
  npIndependentClaim          String?
  npDependentClaim            String?
  npBroadenedFeature          String?
  npIsProfit                  String?
  npIsDefensive               String?
  npAllDrafts                 String[] @default([])
  npDraftingEffort            String?
  npDrafterEmpId              String?
  npHoursSpent                String?
  npAgencyRecognizer          String?
  npAgencyCost                String?
  npReviewEffort              String?
  npManagerEmpId              String?
  npActivityStatus            String?

  // PCT fields
  isDirectPCT                 String?
  pctParentPermission         String[] @default([])
  pctProvisionalDate          String?
  pctApplicationNumber        String?
  pctDrafterName              String?
  pctClaimSheet               String[] @default([])
  pctFormsPrepared            String?
  pctCountryFiling            String?
  pctReviewBy                 String?
  pctCitedPatent              String?
  pctIndependentClaim         String?
  pctDependentClaim           String?
  pctBroadenedFeature         String?
  pctIsProfit                 String?
  pctIsDefensive              String?
  pctAllDrafts                String[] @default([])
  pctDraftingEffort           String?
  pctDrafterEmpId             String?
  pctHoursSpent               String?
  pctAgencyRecognizer         String?
  pctAgencyCost               String?
  pctReviewEffort             String?
  pctManagerEmpId             String?
  pctActivityStatus           String?

  // Complete fields
  isProvisionalFiled          String?
  provisionalSpecDate         String?
  applicationNumber           String?
  isPCTFiled                  String?
  pctFilingDate               String?
  isPCTPublished              String?
  citedPatent                 String?
  independentClaim            String?
  dependentClaim              String?
  broadenedFeature            String?
  isProfitPatent              String?
  isDefensivePatent           String?
  draftVersions               String[] @default([])
  draftingEffort              String?
  drafterEmpId                String?
  hoursSpent                  String?
  agencyRecognizer            String?
  agencyCost                  String?
  reviewEffort                String?
  managerEmpId                String?

  // Provisional fields
  nodrafter                   String?
  noreviewer                  String?
  attachments                 String[] @default([])
  bned                        String?
  ifdescribed                 String?
  toinvention                 String?
  esfd                        String?
  pdrafter                    String?
  nohspent                    String?
  eafd                        String?
  csoagency                   String?
  eihfr                       String?
  mres                        String?

  invention      Invention @relation("PatentSpecificInfoToInvention", fields: [asset_id], references: [asset_id], onDelete: Cascade)
}

model PatentFiling {
  id             String    @id @default(cuid()) // Primary key
  asset_id       String // Foreign key
  activityStatus String?
  rating         Int?
  draftType      String?
  
  // ExtractorDetailsV2 fields (Patent Filing Extractor Details)
  extractorOne               String?
  extractortwo               String?
  iEDate                     String?
  iawpl                      String?
  idattachments              String[] @default([])
  
  // Innovation Analysis fields
  trainRun                   String?
  minuteOfMeeting            String[] @default([])
  attachments                String[] @default([])
  
  // Patentability Extractor fields
  psone                      String?
  pstwo                      String?
  collaboration              String?
  paNfeature                 String?
  paIfeature                 String?
  paScountry                 String?
  paOoextractor              String?
  
  // Decision Sheet fields
  nodc                       String?
  dibrief                    String?
  
  // Average Rating fields
  patentApplicationNumber    String?
  averageRating             Int?
  
  // Patentability Extractor Rating field
  patentabilityRating       Int?
  
  // Effort Sheet fields
  ipRecognizer               String?
  reviewEffort               String?
  managerEmpId               String?
  extractionEffort           String?
  
  // Provisional fields
  patentFilingName           String?
  provisionalPatent          String[] @default([])
  attachment                 String[] @default([])
  dateProvision              String?
  applicantName              String?
  isProfilePatent            String?
  isDefensivePatent          String?
  claimingStartup            String[] @default([])
  poaOffice                  String?
  effortsSpent               String?
  patentFiler                String?
  hoursSpent                 String?
  agencyRecognizer           String?
  agencyCost                 String?
  managerResponsible         String?

  // PCT fields
  postDated                  String?
  applicationProvisionalNumber String?
  datePatentApplication      String?
  pctFilingPermission        String?
  
  // Additional PCT fields
  pctAgencyCost              String?
  pctAgencyRecognizer        String?
  pctApplicationNumber       String?
  pctBroadenedFeature        String?
  pctCitedPatent             String?
  pctCountryFiling           String?
  pctDependentClaim          String?
  pctDrafterEmpId            String?
  pctDrafterName             String?
  pctDraftingEffort          String?
  pctHoursSpent              String?
  pctIndependentClaim        String?
  pctIsProfit                String?
  pctManagerEmpId            String?
  pctReviewBy                String?
  pctReviewEffort            String?
  
  // Additional Patentability Analysis fields
  nfeature                   String?
  ifeature                   String?
  oextractor                 String?
  scountry                   String?

  // National Phase fields
  dateProvisionalPatent      String?
  dateCompletePatentApplication String?
  datePCTPatentApplication   String?
  finalSubmitted             String?
  filedForms                 String[] @default([])
  
  // Additional National Phase fields
  npAgencyCost               String?
  npAgencyRecognizer         String?
  npApplicationCountry       String?
  npApplicationNumber        String?
  npBroadenedFeature         String?
  npCitedPatent              String?
  npDependentClaim           String?
  npDrafterEmpId             String?
  npDrafterName              String?
  npDraftingEffort           String?
  npFormsPrepared            String?
  npHoursSpent               String?
  npIndependentClaim         String?
  npIsDefensive              String?
  npIsProfit                 String?
  npManagerEmpId             String?
  npPCTDate                  String?
  npPCTOrProvisionalDate     String?
  npPCTPublication           String?
  npReviewBy                 String?
  npReviewEffort             String?

  // Complete fields
  dateOfPatent               String?
  provisionalNumber          String?
  specificationFiling        String?
  agentFiling                String?
  filedDraft                 String[] @default([])
  filedFormsComplete         String[] @default([])
  dateOfComplete             String?
  isPostDated                String?
  
  // Additional Complete Specification fields
  isProvisionalFiled         String?
  provisionalSpecDate        String?
  applicationNumber          String?
  isPCTFiled                 String?
  pctFilingDate              String?
  citedPatent                String?
  independentClaim           String?
  dependentClaim             String?
  broadenedFeature           String?
  draftingEffort             String?
  drafterEmpId               String?
  
  // Additional Provisional Application fields
  nodrafter                  String?
  noreviewer                 String?
  bned                       String?
  ifdescribed                String?
  toinvention                String?
  esfd                       String?
  pdrafter                   String?
  nohspent                   String?
  eafd                       String?
  csoagency                  String?
  eihfr                      String?
  mres                       String?
  
  // Additional PCT Application fields
  isDirectPCT                String?
  pctProvisionalDate         String?

  invention      Invention @relation("PatentFiling", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model PostGrantOpposition {
  id                      String  @id @default(cuid())
  asset_id                String
  
  // PGO Extractor Details fields (from old ExtractorDetails component)
  extractorOne            String?
  extractortwo            String?
  iEDate                  String?
  iawpl                   String?
  nfeature                String?
  ifeature                String?
  idattachments           String[] @default([])
  scountry                String?
  oextractor              String?
  
  // PGO Innovation Analysis fields (from old Innovation component)
  trainRun                String?
  minuteOfMeeting         String[] @default([])
  innovationAttachments   String[] @default([])
  
  // PGO Patentability Extractor fields (from old PAExtractor component)
  psone                   String?
  pstwo                   String?
  rating                  Int?
  collaboration           String?
  paNovelFeature          String?
  paInventiveFeature      String?
  paSpecificCountry       String?
  paOpinionOfExtractor    String?
  patentabilityAttachments String[] @default([])
  
  // PGO Decision Sheet fields (from old DecisionSheet component)
  nodc                   String?
  dibrief                String?
  decisionAttachments     String[] @default([])
  
  // PGO fields matching old formData7 structure
  patentApplicationNumber String?
  patentPublished         String?
  publicationNumber       String?
  apopposed              String?
  oname                  String?
  opposerAttachment      String[] @default([])
  cfbopposer             String?
  boaof                  String?
  rffo                   String?
  responseAttachment     String[] @default([])
  orpby                  String?
  eagency                String?
  revby                  String?
  reviewAttachment       String[] @default([])
  ipRecognizer           String?
  hoursSpent             String?
  agencyRecognizer       String?
  agencyCost             String?
  reviewEffort           String?
  reviewEffortHours      String?
  managerEmpId           String?
  activityStatus         String?

  invention Invention @relation("PostGrantOppositionToInvention", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model PatentProsecution {
  id                        String  @id @default(uuid())
  asset_id                  String
  
  // PP Invention Details fields (ppid_)
  ppid_title                    String?
  ppid_common_name              String?
  ppid_inventor_details         String?
  ppid_entity                   String?
  ppid_date                     String?
  ppid_country                  String?
  ppid_creation_country         String?
  ppid_collaboration            String?
  ppid_collaborator_name        String?
  ppid_collaborator_country     String?
  ppid_stakeholders             String?
  
  // PP Extractor Details fields (pped_)
  pped_one                      String?
  pped_two                      String?
  pped_extraction_date          String?
  pped_available_with_prior_literature String?
  pped_novel_feature            String?
  pped_inventive_feature        String?
  pped_specific_country         String?
  pped_opinion                  String?
  pped_attachments              String[] @default([])
  
  // PP Patent Prosecution Details fields (pppd_)
  pppd_published                 String?
  pppd_publication_number        String?
  pppd_any_person_opposed        String?
  pppd_opponent_name            String?
  pppd_attachments               String[] @default([])
  pppd_response_attachments      String[] @default([])
  pppd_review_attachments        String[] @default([])
  pppd_case_filed_by_opposer     String?
  pppd_basis_of_action_of_filing String?
  pppd_reason_for_filing_opposition String?
  pppd_opinion_rendered_by_you   String?
  pppd_external_agency          String?
  pppd_reviewed_by               String?
  
  // PP Patent Application Status fields (ppas_)
  ppas_status                   String?
  ppas_number                   String?
  ppas_attachment               String[] @default([])
  ppas_grant_date               String?
  ppas_rejection_reason_attachment String[] @default([])
  
  // PP FER fields (ppfer_) - as JSON array
  ppfer_list                    Json?
  
  // PP Hearing fields (pph_) - as JSON array
  pph_list                      Json?
  
  // PP Decision Sheet fields (ppds_)
  ppds_name_of_decision_maker   String?
  ppds_decision_in_brief        String?
  ppds_attachments               String[] @default([])
  
  // PP Innovation Analysis fields (ppi_)
  ppi_more_than_invention       String?
  ppi_prior_art_documents       String[] @default([])
  ppi_npl_documents             String[] @default([])
  
  // More legacy field names (for backward compatibility with components)
  ferList                       Json?
  hearingList                   Json?
  decisionNodc                  String?
  decisionDibrief               String?
  decisionAttachments           String[] @default([])
  trainRun                      String?
  minuteOfMeeting               String[] @default([])
  attachments                   String[] @default([])
  
  // PP Patentability Extractor fields (pppe_)
  pppe_searcher1                String?
  pppe_searcher2                String?
  pppe_rating                   Int?
  pppe_invention_accordance      String?
  pppe_novel_feature            String?
  pppe_inventive_feature        String?
  pppe_attachment               String[] @default([])
  pppe_specific_country         String?
  pppe_opinion_of_extractor     String?
  
  // PP Average Patentability Rating fields (ppapr_)
  ppapr_rating                  Int?
  ppapr_patent_application_number String?
  
  // PP Effort Sheet fields (ppes_)
  ppes_ip_recognizer            String?
  ppes_hours_spent              String?
  ppes_agency_recognizer        String?
  ppes_agency_cost              String?
  ppes_review_effort            String?
  ppes_manager_emp_id           String?
  
  // PP Activity Status fields (ppact_)
  ppact_status                  String?
  ppact_description             String?
  ppact_last_updated            String?

  // Final legacy field names (for backward compatibility with components)
  rating                        Int?
  patentApplicationNumber       String?
  ppEffortSheetIpRecognizer    String?
  ppEffortSheetHoursSpent      String?
  ppEffortSheetAgencyRecognizer String?
  ppEffortSheetAgencyCost       String?
  ppEffortSheetReviewEffort     String?
  ppEffortSheetManagerEmpId     String?

  invention        Invention @relation("PatentProsecution", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model PatentMaintenance {
  id                      String  @id @default(cuid())
  asset_id                String
  
  // PM Extractor Details fields (same as PGO but separate storage)
  extractorOne            String?
  extractortwo            String?
  iEDate                  String?
  iawpl                   String?
  nfeature                String?
  ifeature                String?
  idattachments           String[] @default([])
  scountry                String?
  oextractor              String?
  
  // PM Patent Prosecution Details fields (same as PGO but separate storage)
  patentPublished         String?
  publicationNumber       String?
  apopposed              String?
  oname                  String?
  opposerAttachment      String[] @default([])
  cfbopposer             String?
  boaof                  String?
  rffo                   String?
  responseAttachment     String[] @default([])
  orpby                  String?
  eagency                String?
  revby                  String?
  reviewAttachment       String[] @default([])
  
  // PM Patent Maintenance History fields
  priorityDate           String?
  grantDate              String?
  yearsPaid              String?
  nextDueDate            String?
  maintenanceStopped     String?
  attachments            String[] @default([])
  collaboration          String?
  filingDate             String?
  filingAttachments      String[] @default([])
  maintenanceFee         String?
  externalAgency         String?
  
  // PM fields matching old formData8 structure
  patentApplicationNumber String?
  ipRecognizer           String?
  hoursSpent             String?
  agencyRecognizer       String?
  agencyCost             String?
  reviewEffort           String?
  managerEmpId           String?
  reviewEffortHours      String?
  activityStatus         String?
  
  // PM Decision Sheet fields
  nodc                   String?
  dibrief                String?
  decisionAttachments    String[] @default([])
  
  // PM Innovation Analysis fields
  trainRun               String?
  minuteOfMeeting        String[] @default([])
  innovationAttachments  String[] @default([])
  
  // PM Patentability Extractor fields
  psone                  String?
  pstwo                  String?
  rating                 Int?
  paNovelFeature         String?
  paInventiveFeature     String?
  paSpecificCountry      String?
  paOpinionOfExtractor   String?
  patentabilityAttachments String[] @default([])

  invention Invention @relation("PatentMaintenanceToInvention", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model PatentCommercialisation {
  id       String @id @default(uuid())
  asset_id String
  
  // PC Invention Details fields (pcInventionDetails prefix)
  pcInventionDetailsTitle                    String?
  pcInventionDetailsCommonName               String?
  pcInventionDetailsInventorDetails          String?
  pcInventionDetailsEntity                   String?
  pcInventionDetailsDate                     String?
  pcInventionDetailsCountry                  String?
  pcInventionDetailsCreationCountry          String?
  pcInventionDetailsCollaboration            String?
  pcInventionDetailsCollaboratorName         String?
  pcInventionDetailsCollaboratorCountry      String?
  pcInventionDetailsStakeholders             String?
  
  // PC Patent Commercialization Child fields (pcPatentCommercializationChild prefix)
  pcPatentCommercializationChildStage String?
  pcPatentCommercializationChildWorkingFiled String?
  pcPatentCommercializationChildImplementationFile String[] @default([])
  pcPatentCommercializationChildFirstWorkingDate String?
  pcPatentCommercializationChildCommercializationStatus String?
  pcPatentCommercializationChildRevenueGenerated String?
  pcPatentCommercializationChildMarketValue String?
  pcPatentCommercializationChildLicensingFee String?
  pcPatentCommercializationChildRoyaltyRate String?
  pcPatentCommercializationChildPartnerName String?
  pcPatentCommercializationChildPartnershipType String?
  pcPatentCommercializationChildPartnershipDetails String?
  pcPatentCommercializationChildStartDate String?
  pcPatentCommercializationChildExpectedCompletionDate String?
  pcPatentCommercializationChildActualCompletionDate String?
  
  // PC PAN fields (pcPAN prefix)
  pcPANPatentApplicationNumber               String?
  pcPANPatentNumber String?
  
  // PC Patent Commercialization Efforts fields (pcPatentCommercializationEfforts prefix)
  pcPatentCommercializationEffortsSalesFile String[] @default([])
  pcPatentCommercializationEffortsPeriodicSales String?
  pcPatentCommercializationEffortsInvoiceFile String[] @default([])
  pcPatentCommercializationEffortsCommercializationDate String?
  pcPatentCommercializationEffortsProductId String?
  pcPatentCommercializationEffortsIsLicensed String?
  pcPatentCommercializationEffortsIsCrossLicensed String?
  pcPatentCommercializationEffortsIsCompulsoryLicenseFiled String?
  
  // PC Activity Status fields (pcActivityStatus prefix)
  pcActivityStatusStatus                     String?
  pcActivityStatusDescription                String?
  pcActivityStatusLastUpdated                String?

  invention Invention @relation("PatentCommercialisation", fields: [asset_id], references: [asset_id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
